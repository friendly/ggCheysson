% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cheysson_name.R
\name{cheysson_name}
\alias{cheysson_name}
\title{Translate a palette label from another naming scheme to the current name}
\usage{
cheysson_name(
  label,
  from = c("auto", "name", "andrews", "shanley", "rumsey_no", "old_name"),
  advent_day = NULL
)
}
\arguments{
\item{label}{Character string to look up.}

\item{from}{Which naming scheme \code{label} is in:
\itemize{
\item \code{"auto"} (default): try, in order, the current \code{name}, \code{andrews_label},
\code{shanley_id}, \code{rumsey_no}, and finally \code{old_name} (see Details for why
\code{old_name} is tried last and can be ambiguous)
\item \code{"name"}: the package's current palette name
\item \code{"andrews"}: RJ Andrews' label, e.g. "Dec.01-1883.21"
\item \code{"shanley"}: Tom Shanley's Observable ID, e.g. "diverging12514021"
\item \code{"rumsey_no"}: David Rumsey catalog number, e.g. "12514.021"
\item \code{"old_name"}: the name this palette had before the 1.1.0 naming fix -
see Details, this can be ambiguous
}}

\item{advent_day}{Optional advent day (1-25) to disambiguate when \code{label}
alone matches more than one palette (only possible for
\code{from = "old_name"}/\code{"auto"}), or to assert which palette you mean even
when \code{label} is already unambiguous. An \code{advent_day} that contradicts
what \code{label} resolves to is an error.}
}
\value{
A character string: the current palette name.
}
\description{
Looks up a palette by whichever label you have it in - the package's own
(possibly outdated) name, RJ Andrews' Advent-calendar label, Tom Shanley's
Observable notebook ID, a David Rumsey catalog number, or an advent day -
and returns the current name to pass to \code{\link[=scale_color_cheysson]{scale_color_cheysson()}},
\code{\link[=cheysson_pal]{cheysson_pal()}}, \code{\link[=cheysson_pattern]{cheysson_pattern()}}, and friends.
}
\details{
Before ggCheysson 1.1.0, 4 different \code{old_name} values were each used by
2-3 genuinely different palettes (a naming collision bug - see
\link{cheysson_labels}), so translating from \code{old_name} alone can be
ambiguous. When it is, \code{cheysson_name()} returns the palette that
actually shipped under that name in 1.0.1 (the one with the highest
\code{advent_day} among the candidates - this is what the pre-fix extraction
logic itself picked) and issues a \code{warning()} listing every candidate it
did \emph{not} return. Pass \code{advent_day} explicitly to get a different one
without a warning.
}
\examples{
# RJ Andrews' label -> current name
cheysson_name("Dec.01-1883.21")

# Tom Shanley's ID -> current name
cheysson_name("diverging12514021")

# An old (pre-1.1.0) name that's unambiguous
cheysson_name("1883_04", from = "old_name")

# An old name that collided across 2 palettes: returns the one that
# shipped under this name in 1.0.1, with a warning
cheysson_name("1880_07", from = "old_name")

# Disambiguate explicitly - no warning
cheysson_name("1880_07", from = "old_name", advent_day = 6)

}
\seealso{
\link{cheysson_labels}
}
