% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_cheysson}
\alias{theme_cheysson}
\title{Cheysson theme for ggplot2}
\usage{
theme_cheysson(
  base_size = 11,
  base_family = "auto",
  title_family = "auto",
  axis_title_family = "auto",
  load_fonts = TRUE
)
}
\arguments{
\item{base_size}{Base font size (default: 11)}

\item{base_family}{Base font family. If "auto" (default), uses Cheysson if
available, otherwise falls back to sans-serif}

\item{title_family}{Font family for titles (default: "auto")}

\item{axis_title_family}{Font family for axis titles (default: "auto")}

\item{load_fonts}{Automatically load Cheysson fonts if not already loaded
(default: TRUE)}
}
\value{
A ggplot2 theme object
}
\description{
A ggplot2 theme inspired by the visual style of the Albums de Statistique
Graphique, using Cheysson fonts and appropriate styling.
}
\details{
This theme applies the following styling:
\itemize{
\item Cheysson fonts for all text elements
\item Minimal grid lines
\item Classic axis styling
\item Subtle colors matching historical aesthetics
}

Font selection:
\itemize{
\item Plot title: CheyssonTitle (decorative)
\item Axis titles: CheyssonSansCaps (capitals)
\item Body text: Cheysson (regular)
}
}
\examples{
\dontrun{
library(ggplot2)

# Load fonts first (required for proper rendering)
load_cheysson_fonts()

# Basic usage
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  labs(title = "Automobile Statistics") +
  theme_cheysson()

# With Cheysson color palette
ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
  geom_point(size = 3) +
  scale_color_cheysson("1881_04") +
  theme_cheysson()
}

}
